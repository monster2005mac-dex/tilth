// config.example.js — safe to commit.
// Copy this file to config.js, fill in your real Groq API keys, and
// keep config.js OUT of git (it's already in .gitignore).

window.GROQ_API_KEYS = [
  "PASTE_YOUR_GROQ_API_KEY_HERE",
];
